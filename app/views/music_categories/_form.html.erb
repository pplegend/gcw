<%= form_for(@music_category) do |f| %>
  <% if @music_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_category.errors.count, "error") %> prohibited this music_category from being saved:</h2>

      <ul>
      <% @music_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
