<div class="span4">
	<p class="alert alert-info"><%= @newest_title %></p>
	<ul class="nav nav-pills nav-stacked">
	<% @newest_contents.each do |content| %>
		<li>
		<%= link_to content.name, content %> 
		</li>
	<% end %>
	</ul>
</div>
