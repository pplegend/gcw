<%= form_for(@post) do |f| %>

    <%= f.hidden_field :topic_id, :value=>@topic %>
    <%= f.hidden_field :user_id, :value=>current_user %>
  
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
